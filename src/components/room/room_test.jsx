import React from 'react';
import PropTypes from 'prop-types';

const Room = ({props}) => {
  console.log(props);
  return (
    <div>Hello world!</div>
  );
};

Room.propTypes = {
}

export default Room;
